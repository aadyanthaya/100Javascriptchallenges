function arrayPostive(input)
{let n = input.length;
  let output = [];
  for(let i =0; i<n ;i++)
  { 
    if(input[i] > 0)
    {output[i] = input[i];}

  }
  return output.filter((k) => k !== undefined);
 }
console.log(arrayPostive([-10,11,-1,-2,5,6,7]));

function lastIndex(input,num)
{
  let n =input.length;
  let output = 0;
  for(let i= 0; i <n ;i++)
  {   if(num === input[i])
      { output = i}}

      if (output === 0)
      {return -1;}
      return output;
}
console.log(lastIndex([2,3,1,4,2,5,5],8))
